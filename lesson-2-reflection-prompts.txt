What happens when you initialize a repository? Why do you need to do it?
the git will create the local repository on the system.and it is going to tell Git to initialize the project to get everything ready to start doing its tracking.

How is the staging area different from the working directory and the repositor.What value do you think it offers?

if we want to add multiple files of code. but we have perform only one logical change in multiple file then we can put that multiple file in staging area and then commit at once into the repository. we cannot do it directly from working directory.

How can you use the staging area to make sure you have one commit per logical change?
if i am working on one logical change in multiple files then i can pass those file into the staging area and then commit as all the file in the staging area will represent one logical change.

What are some situations when branches would be helpful in keeping your history
organized? How would branches help?

if i want to try some new thing but i dont want to disturb main branch then i can use branches for that. it help me to keep my original content as it is. and i can try out new thing without worring.


How do the diagrams help you visualize the branch structure?

The diagram is helpful to understand how branch is structure and helps us to clarify the structure to operate on it.

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?

both programmer can work seperately and merge their features.  the diagram gives us the clear sturcture how both programmer has worked on different branch.

What are the pros and cons of Git's automatic merging vs. always doing merges
manually?
The git automatic merging is a awesome feature when we dont have the conflits.Everything can be done smoothly.We can merge many features without to much work.but when there is a conflit we have to ask the another programmer and analyze and understand the other programmer code.this is bit time consuming. 